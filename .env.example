# Environment Variables for La Baguette Magique

# JWT Secret (REQUIRED)
# Generate a strong random string for production
# Example: openssl rand -base64 32
JWT_SECRET=your-super-secret-jwt-key-change-this-in-production

# Admin Username (optional, defaults to "admin")
ADMIN_USERNAME=admin

# Admin Password Hash (REQUIRED)
# Generate using: node -e "require('bcryptjs').hash('your-password', 10).then(console.log)"
# IMPORTANT: Never use plain text passwords. Always use bcrypt hash.
ADMIN_PASSWORD_HASH=

# Node Environment
NODE_ENV=development